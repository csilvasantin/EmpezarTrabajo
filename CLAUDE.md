# Proyecto 01 — EmpezarTrabajo

## 1 2 3 basico

1. ejecutar `./start_work.sh`;
2. abrir `onboarding` y hacer onboarding;
3. terminar con HTML y URL publica comprobable.

Este repositorio sirve para empezar una sesion de trabajo.

Flujo recomendado:

1. ejecutar `./start_work.sh`;
2. abrir `csilvasantin/onboarding`;
3. hacer onboarding;
4. continuar con el repo de trabajo correcto.

No improvisar contexto si ya esta documentado en `onboarding`.

Si se usa `Nomeacuerd0` en una URL publica, tratarlo solo como barrera ligera.
